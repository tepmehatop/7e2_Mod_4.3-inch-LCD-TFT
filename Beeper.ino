
// ********** Beeper ********** 
void Beep()         // одинн коротий сигнал 
{
   Beeper_On();
   _delay_ms(20);
   Beeper_Off();
}

void BeepBeep()     // два коротких сигнала
{
   Beeper_On();
   _delay_ms(50);
   Beeper_Off();   
   _delay_ms(100);
   Beeper_On();
   _delay_ms(50);
   Beeper_Off();
}

void BeepComplete()  // три длинных сигнала
{
   Complete_flag = true;
   Print();
   Beeper_On();
   _delay_ms(500);
   Beeper_Off();   
   _delay_ms(100);
   Beeper_On();
   _delay_ms(500);
   Beeper_Off();
   _delay_ms(100);
   Beeper_On();
   _delay_ms(500);
   Beeper_Off();
}

void Beep_Error()    // один длинный сигнал
{
   Print();
   Beeper_On();
   _delay_ms(400);
   Beeper_Off();
}
